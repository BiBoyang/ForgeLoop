# BATCH-001：STEP-007 ~ STEP-009 批量开发包

## 适用场景
- 你希望一次性推进多个能力点，再集中做一轮 review。
- 目标是先打通“真实工具闭环 + read/write + bash 前台”。

## 批量范围（本包）
- `STEP-007`：AgentLoop 真实工具执行闭环
- `STEP-008`：read/write 工具 + cwd 安全约束
- `STEP-009`：bash 工具（前台）+ timeout/cancel

## 建议执行顺序（强约束）
1. 先做 `STEP-007` 的基础抽象（ToolExecutor + tool_result 注入）；
2. 在同一抽象上落地 `STEP-008` read/write；
3. 最后补 `STEP-009` bash 与超时取消；
4. 全部完成后再统一补测试与回归。

## 最小交付清单（你执行时逐项勾选）
- [ ] AgentLoop 不再只发占位事件，而是执行真实工具；
- [ ] assistant(tool_call) 与 tool_result 顺序正确入上下文；
- [ ] read/write 工具可用，且越界路径被拒绝；
- [ ] bash 工具可用，超时和取消能正确结束；
- [ ] `swift test --filter Agent` 通过；
- [ ] `swift test --filter AI` 通过；
- [ ] `swift test` 全量通过。

## 统一回归命令（建议）
- `swift test --filter AgentLoopToolExecution`
- `swift test --filter BuiltinReadWriteTool`
- `swift test --filter BashTool`
- `swift test --filter Agent`
- `swift test --filter AI`
- `swift test`

## 提交节奏（建议）
- 你可分 2~3 个 commit（按 step 切分），但一次提 review。
- 提 review 时给我：
  - 关键设计说明（尤其 tool_result 结构）；
  - 改动文件清单；
  - 上述命令的结果摘要。
