# BATCH-003：STEP-017 ~ STEP-019 批量开发包

## 适用场景
- 你希望优先补并发与执行引擎能力，再做 UI/发布层。
- 目标是“稳定 + 可扩展”的工具执行底座。

## 批量范围（本包）
- `STEP-017`：后台任务可取消与进程回收
- `STEP-018`：工具参数校验与错误模型统一
- `STEP-019`：工具并发策略切换（sequential/parallel）

## 建议执行顺序（强约束）
1. `STEP-017`（先补取消一致性）；
2. `STEP-018`（统一错误模型，减少后续反复改）；
3. `STEP-019`（最后做并发策略，变更面最大）。

## 最小交付清单
- [ ] 后台取消真实终止进程且状态稳定；
- [ ] 工具错误输出结构统一；
- [ ] 并发策略可切换且结果顺序不乱；
- [ ] `swift test --filter Agent` 通过；
- [ ] `swift test` 全量通过。

## 统一回归命令（建议）
- `swift test --filter BackgroundTask`
- `swift test --filter Tool`
- `swift test --filter AgentLoopToolExecution`
- `swift test --filter Agent`
- `swift test`
