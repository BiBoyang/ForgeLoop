# BATCH-002：STEP-011 ~ STEP-013 批量开发包

## 适用场景
- 你希望在完成 `STEP-010` 后，连续补齐“渲染数据 + 编辑 + 检索工具”。
- 目标是让工具体验从“可用”进入“高可用”。

## 批量范围（本包）
- `STEP-011`：tool 结果摘要透传（给 TUI 渲染）
- `STEP-012`：`edit` 工具最小落地
- `STEP-013`：`ls/find/grep` 工具最小落地

## 建议执行顺序（强约束）
1. 先做 `STEP-011`（先打通数据结构与事件负载）；
2. 再做 `STEP-012`（局部编辑能力）；
3. 最后做 `STEP-013`（检索能力，输出与限制策略更复杂）。

## 最小交付清单（你执行时逐项勾选）
- [ ] `toolExecutionEnd` 可携带 `summary` 并在 TUI 展示；
- [ ] `edit` 工具可在 cwd 内正确替换文本；
- [ ] `ls/find/grep` 可用且遵守 cwd 安全约束；
- [ ] 大输出有截断或数量上限；
- [ ] `swift test --filter Agent` 通过；
- [ ] `swift test --filter TranscriptRenderer` 通过；
- [ ] `swift test` 全量通过。

## 统一回归命令（建议）
- `swift test --filter TranscriptRenderer`
- `swift test --filter AgentLoopToolExecution`
- `swift test --filter EditTool`
- `swift test --filter BuiltinSearchTools`
- `swift test --filter Agent`
- `swift test`

## 提交节奏（建议）
- 建议分 3 个 commit（011/012/013 各一个），但一次提 review。
- 提 review 时附：
  - 关键结构变更（特别是 `AgentEvent` 字段）；
  - 改动文件清单；
  - 回归结果摘要。
