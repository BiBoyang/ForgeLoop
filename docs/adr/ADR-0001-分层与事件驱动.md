# ADR-0001：分层与事件驱动

## 状态
Accepted

## 背景
需要复刻 KWWK，且重点要求 Harness Agent 与 TUI 流式行为一致。

## 决策
- 采用三层结构：`ForgeLoopAI` / `ForgeLoopAgent` / `ForgeLoopCli`。
- 采用事件驱动渲染：Provider 事件不直接触屏，统一经 AgentEvent 到 UI。

## 结果
- 优点：模块清晰、可测试、流式一致性高。
- 代价：前期抽象较多，需要严格遵守事件边界。
