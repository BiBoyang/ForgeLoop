# 事件链路（核心）

Provider SSE
-> `AssistantMessageEvent`
-> `AgentEvent.messageUpdate`
-> `TranscriptRenderer.apply`
-> `TUI.requestRender`

## 关键不变量
- 每次 assistant 回复必须完整闭环：start/update*/end。
- `AgentEvent` 是 UI 的唯一事实来源。
- UI 渲染不能修改 Agent 状态，只能消费事件。
