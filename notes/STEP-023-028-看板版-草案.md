# STEP-023~028 看板版（讨论草案）

状态说明：`Todo` / `Ready` / `In Progress` / `Review` / `Done`

## 看板表

| Step ID | 标题 | 状态 | 负责人 | 依赖 | 备注 |
|---|---|---|---|---|---|
| STEP-023 | TUI 渲染内核升级（inline retained-mode） | Ready | 你 | 无 | 先追平终端体感，替换全屏清空重绘 |
| STEP-024 | 输入链路重构（raw stdin + keybinding） | Todo | 你 | STEP-023 | 替换 `readLine()`，补 ESC flush 与 bracketed paste |
| STEP-025 | 组件化布局落地（Header/Transcript/Status/Queue/Input） | Todo | 你 | STEP-023, STEP-024 | 从“拼 frame”切到“组件树 + 布局器” |
| STEP-026 | Transcript 语义增强 | Todo | 你 | STEP-025 | 补齐 thinking/toolCall 去重/uiDisplay/通知折叠 |
| STEP-027 | 登录与鉴权闭环（可商用门槛） | Todo | 你 | 无（建议并行） | `login + AuthResolver` 端到端可用 |
| STEP-028 | 测试与性能门禁 | Todo | 你 | STEP-023~027 | 建立 TUI 回归与性能基线，形成 CI 门禁 |

## 依赖关系（建议执行顺序）

- 主链路：`STEP-023 -> STEP-024 -> STEP-025 -> STEP-026 -> STEP-028`
- 并行链路：`STEP-027` 可在 `STEP-024` 后并行推进（减少总工期）
- 收口：`STEP-028` 作为统一验收与门禁落地步骤

## 每步建议验证命令（草案）

### STEP-023

- `swift test --filter TUIRunnerTests`
- `swift test --filter TranscriptRendererTests`
- `swift test --filter ForgeLoopCliTests`

### STEP-024

- `swift test --filter TUIRunnerTests`
- `swift test --filter PromptControllerTests`
- `swift test --filter ForgeLoopCliTests`

### STEP-025

- `swift test --filter TranscriptRendererTests`
- `swift test --filter ForgeLoopCliTests`
- `swift test --filter AgentStabilityTests`

### STEP-026

- `swift test --filter TranscriptRendererTests`
- `swift test --filter TranscriptRendererToolResultTests`
- `swift test --filter ForgeLoopCliTests`

### STEP-027

- `swift test --filter AuthAndLabelTests`
- `swift test --filter ModelStoreTests`
- `swift test --filter ForgeLoopCliTests`

### STEP-028

- `swift test --filter Agent`
- `swift test --filter AI`
- `swift test`

## 讨论闸门（执行前确认）

- 渲染策略：inline-only 先落地，还是 inline + alternate 同时支持
- 输入范围：首批仅核心键位，还是功能键一起纳入
- RenderEvent 层：继续轻量适配，还是回归直接消费 AgentEvent
- 登录策略：单 provider 先闭环，还是多 provider 同步推进
- 性能门禁：先“记录趋势”还是“阈值失败即阻断”
